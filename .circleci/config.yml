version: 2.1

jobs:
  build:
    docker:
      - image: docker
    steps:
      - checkout
      - run: 
          name: Current working directory
          command: echo $(pwd)
      - run: 
          name: Install docker
          command: |
            docker --version
            docker container ls
      # - run:
      #     name: Run docker hello-world
      #     command: docker run hello-world